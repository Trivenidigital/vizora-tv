{
  "name": "vizora-tv",
  "version": "1.0.0",
  "private": true,
  "description": "Vizora Digital Signage Display Client for Android TV",
  "author": "Vizora <support@vizora.io>",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:init": "cap init \"Vizora Display\" \"com.vizora.display\" --web-dir dist",
    "cap:add:android": "cap add android",
    "cap:sync": "cap sync",
    "cap:open": "cap open android",
    "cap:copy": "cap copy",
    "cap:update": "cap update",
    "android:dev": "npm run build && cap sync android && cap open android",
    "android:build": "npm run build && cap sync android",
    "android:run": "npm run build && cap sync android && cap run android",
    "android:run:device": "npm run build && cap sync android && cap run android --target",
    "test": "vitest run",
    "test:watch": "vitest",
    "postinstall": "echo Run: npm run cap:init then npm run cap:add:android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/filesystem": "^6.0.0",
    "@capacitor/network": "^6.0.0",
    "@capacitor/preferences": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0",
    "qrcode": "^1.5.3",
    "socket.io-client": "^4.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "terser": "^5.46.0",
    "vitest": "^3.2.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  }
}
