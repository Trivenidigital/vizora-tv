<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions (C23: must appear before <application>) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Exact alarms: auto-granted on API 31-32. USE_EXACT_ALARM is deliberately
         NOT declared — Play policy restricts it to alarm/clock/calendar apps.
         On API 33+ CrashRecoveryHandler falls back to a windowed inexact alarm. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:maxSdkVersion="32" />

    <!-- Android TV Features -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:banner="@drawable/tv_banner"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true"
            android:screenOrientation="sensorLandscape">

            <!--
              DO NOT add android:keepScreenOn="true" here. It was present until
              1.3.12 and did nothing: keepScreenOn is a View attribute, so it is
              accepted by the manifest namespace but never read from ActivityInfo.
              It looked like the screen was being kept awake for a 24/7 signage
              app while the device followed its normal sleep/screensaver policy.

              The screen is kept awake by FLAG_KEEP_SCREEN_ON on the Activity
              window — see MainActivity.onCreate().
            -->

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Boot Receiver for Auto-Start (C10: add permission) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- F55: OEM quick-boot / fast-boot broadcasts (some TV boxes send these
                     instead of BOOT_COMPLETED). No directBootAware — the receiver needs
                     credential-encrypted storage, unavailable during direct boot. -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
            <!-- N3: relaunch after our own app is updated (Play or sideload). The update
                 kills the process and nothing restarts it, so the screen stays dark until a
                 power-cycle. MY_PACKAGE_REPLACED is delivered only to the replaced app and
                 carries no data scheme, hence its own filter (unlike PACKAGE_REPLACED).
                 HONESTY: the background-activity-launch restriction applies here exactly as
                 it does to BOOT_COMPLETED — on Android 12+ this may be a no-op. Landed
                 because it cannot regress and it closes the hole on permissive/AOSP boxes;
                 NOT because we have evidence it relaunches on a modern Google TV. -->
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>
</manifest>
